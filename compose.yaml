services:
  sb-relay:
    image: ghcr.io/krabdo/sb-relay:0.1.0
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - sb-relay-data:/data
    security_opt:
      - no-new-privileges:true

volumes:
  sb-relay-data:
